export default function Login() {
  return (
    <div style={{
      height: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      backgroundColor: 'var(--bg)'
    }}>
      <p style={{
        color: 'var(--text)',
        fontSize: '1.1rem',
        letterSpacing: '0.05em'
      }}>
        UPNG-ChatSpace v1.0 — INITIALIZING...
      </p>
    </div>
  )
}
