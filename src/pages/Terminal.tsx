export default function Terminal() {
  return (
    <div style={{
      height: '100%',
      padding: '1.5rem',
      backgroundColor: 'var(--bg)'
    }}>
      <p style={{ color: 'var(--text)' }}>&gt; SYSTEM READY. AWAITING INPUT.</p>
    </div>
  )
}
